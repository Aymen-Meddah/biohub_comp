# requirements-lock.txt
# Exact-pinned versions for reproducible training runs.
# Adjusted for Google Colab: numpy/pandas/tqdm/scipy raised just enough
# to stop conflicting with Colab's pre-installed packages (opencv, jax,
# cupy, rasterio, shap, google-colab, tsfresh, access), while keeping
# torch/monai pinned exactly for training reproducibility.
#
# Usage:
#   pip install -r requirements-lock.txt
#
# Do not bump torch/monai individually -- if you need a newer version,
# upgrade both together and re-run the full test suite plus a few
# training epochs before trusting results.

PyYAML==6.0.3
numpy==2.0.2
pandas==2.2.2
scipy==1.14.1
networkx==3.3
tqdm==4.67.1
zarr==2.18.3
numcodecs==0.15.1
ome-zarr==0.9.0
filterpy==1.4.5
monai==1.4.0
torch==2.4.1
torchvision==0.19.1
torchaudio==2.4.1
geff==0.1.2